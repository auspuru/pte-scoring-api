{
  "name": "pte-scoring-api",
  "version": "19.5.0",
  "description": "PTE SWT Scoring API + Practice Portal — Railway Deployment (v19.5: deep debug pass — local fallback honesty + boundary inclusivity + cohesion heuristic)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@anthropic-ai/sdk": "^0.24.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
