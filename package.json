{
  "name": "pte-scoring-api",
  "version": "19.4.0",
  "description": "PTE SWT Scoring API + Practice Portal — Railway Deployment (v19.4: dynamic content scoring 0-N + atomic writes for data safety)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.3",
    "cors": "^2.8.6",
    "express": "^4.22.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
