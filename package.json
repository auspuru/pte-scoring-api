{
  "name": "pte-scoring-api",
  "version": "19.17.0",
  "description": "PTE SWT Scoring API + Practice Portal \u2014 v19.17: scrypt password hashing, session-token auth on sync, rate limiting, Claude retry + honest fallback, attempt timestamps, print-friendly grammar annotations",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.3",
    "cors": "^2.8.6",
    "express": "^4.22.2",
    "pg": "^8.20.0",
    "express-rate-limit": "^7.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
