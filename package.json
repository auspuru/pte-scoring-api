{
  "name": "pte-scoring-api",
  "version": "19.4.1",
  "description": "PTE SWT Scoring API + Practice Portal — Railway Deployment (v19.4.1: fix reconcile bug — trust ideas_captured array over legacy numeric score)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@anthropic-ai/sdk": "^0.24.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
